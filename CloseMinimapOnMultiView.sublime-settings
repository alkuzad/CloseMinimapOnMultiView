{
  // Defines which layout property will be checked:
  // cells: both rows and columns increments cells count (any layout change will hide minimap)
  // rows: increments when users adds row (when vertical group split is used)
  // cols: increments when users adds columns (when horizontal split is used) - default
  "check_type": "cols"
}